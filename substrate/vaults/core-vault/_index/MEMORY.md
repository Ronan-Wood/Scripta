# core-vault — content map

The shared root vault. Tier 1 (operator) + Tier 2 (grounded reference). Inherits nothing;
it is the root every project reads FROM. Nothing auto-writes INTO it — promoting a project
insight to operator tier is a deliberate manual act.

Read this first on navigation. The engine is the front door; this index is for humans.

## 00-operator (Tier 1)
- [[operator-working-cadence]] — audit → review → implement → verify; the load-bearing loop.

## 10-reference (Tier 2)
- frozen/software-dev/ddia-2e — Designing Data-Intensive Applications; domains
  [software-dev, distributed-systems, databases].
- versioned/software-dev/go/1.21 — Go language specification, version 1.21 (pin-selected).
