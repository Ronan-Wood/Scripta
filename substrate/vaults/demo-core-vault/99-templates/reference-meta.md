---
title: <source title>
class: reference-frozen
status: active
doc_type: reference
domains: [<primary-domain>, <other-domain>]
raw: <source-file>
raw_location: user-defined
---

# <source title> — source metadata

<`_meta.md` sits beside `structure.md` and `passages/`. `domains` is the multi-valued retrieval
axis (the engine reads the tags, not the folder). `class` is reference-frozen (no version) or
reference-versioned (requires a captured version). The `raw` pointer is contract provenance to
the immutable source; its location is the user's to determine.>
