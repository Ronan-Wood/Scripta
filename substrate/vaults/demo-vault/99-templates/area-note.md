---
title: <title>
status: active
doc_type: <decision | explanation | reference | how-to | digest>
domains: [<domain>]
---

# <title>

<An active work thread. Set `status` deliberately: active (live), complete (done and correct,
kept on the surface), archived (complete and filed out of the way), or superseded (replaced —
add `superseded_by: <doc_id>` and keep it in place, linked). `_archive/` is the human view;
`status:` is what the engine reads, and the two must agree. Set `doc_type` (§6a) to the one job
this note does — split it if it does two (WRITING.md rule 8).>
